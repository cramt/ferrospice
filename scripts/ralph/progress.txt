# Ralph Progress Log
Started: Sun  8 Mar 14:08:50 CET 2026
---

## Codebase Patterns
- AC analysis: `simulate_ac(&Netlist) -> Result<SimResult>` in ferrospice-core::ac
- AC uses `ComplexLinearSystem` (separate real/imag sparse matrices) solved via faer `c64` LU decomposition
- AC sweep modes: DEC (n pts/decade, log spacing), OCT (n pts/octave), LIN (n total points, linear)
- AC source excitation: `AcSpec { mag, phase }` on voltage/current sources → complex RHS in branch/node equations
- AC capacitor stamp: imaginary part += ωC (susceptance) between cap nodes
- AC inductor stamp: imaginary part += -ωL on branch diagonal (branch equation)
- AC diode stamp: small-signal conductance g_d at DC OP + junction capacitance Cj(V) + transit time cap τ*g_d
- `DiodeModel::junction_capacitance(v)` computes depletion cap Cj(V) = CJO/(1-V/VJ)^M with linear extrapolation beyond FC*VJ
- Adaptive timestep: LTE estimated from BE vs Trap difference on capacitor charges and inductor fluxes
- `BreakpointTable` collects discontinuity times from waveforms, forces timestep to land on them
- `waveform::breakpoints(&Waveform, &TranParams) -> Vec<f64>` extracts breakpoint times from waveforms
- Breakpoint behavior: at breakpoints, use BE integration and reduce step by 0.1× for stability
- `h_max` = tmax from .tran if specified, otherwise min(tstep, tstop/50) — caps internal timestep
- LTE tolerance: `TRTOL * max(vol_tol, chg_tol)` where TRTOL=7.0 (matching ngspice CKTtrtol)
- Timestep adjustment: reject if new_h < 0.9×step_h, accept and grow up to 2× otherwise
- step_h vs h: step_h is the actual step size used this iteration, h is the suggested next step from LTE
- Waveform evaluation: `waveform::evaluate(&Waveform, t, &TranParams) -> f64` in ferrospice-core::waveform
- `TranParams { tstep, tstop }` provides defaults for waveform parameters (rise/fall times, SIN freq, etc.)
- `VoltageSourceInstance` and `CurrentSourceInstance` in MnaSystem track branch_idx/node indices + waveform
- In transient, after copying base stamps, waveform values override source RHS entries at each timestep
- PULSE defaults: td=0, tr=tstep, tf=tstep, pw=0, per=tr+pw+tf; repeats via time modulo period
- SIN phase parameter is in degrees (converted to radians internally), damping via exp(-theta*(t-td))
- Transient: `simulate_tran(&Netlist) -> Result<SimResult>` in ferrospice-core::transient
- Transient uses BE for first timestep, then switches to Trapezoidal (matching ngspice)
- Capacitor companion model: BE → geq=C/h, ieq=-C/h*v_prev; Trap → geq=2C/h, ieq=-(2C/h*v_prev + i_prev)
- Inductor companion model: BE → req=L/h, veq=-L/h*i_prev; Trap → req=2L/h, veq=-(2L/h*i_prev + v_prev)
- Inductor in transient: adds -req on branch diagonal, veq on branch RHS (on top of topology stamps)
- `CapacitorInstance` and `InductorInstance` in MnaSystem hold resolved values and IC params
- IC= param on C/L elements overrides DC OP initial voltage/current for transient
- Transient output: "time" vector + node voltages + branch currents, plot name "tran1"
- DC sweep: `simulate_dc(&Netlist) -> Result<SimResult>` in ferrospice-core::simulate
- DC sweep reuses assembled MNA matrix, only modifies RHS entries per sweep point
- Voltage source sweep: set `system.rhs[num_nodes + vsource_idx] = sweep_value`
- Current source sweep: undo old stamp, apply new value at node RHS entries
- DC sweep result has sweep variable as first vector (e.g., "v1"), then node voltages and branch currents
- Double sweep: outer loop is src2, inner loop is src1
- `NodeMap::len()` and `NodeMap::is_empty()` are now public
- Integration tests go in `ferrospice-core/tests/` — use `CARGO_MANIFEST_DIR` to locate ngspice test files
- ngspice check.sh filters: SPARSE|KLU|CPU|Dynamic|Note|Circuit|Doing|---|Data|Index|trans|acan|etc.
- ngspice .out data rows are tab-separated, start with integer index, use scientific notation
- `simulate_op` works on any netlist regardless of what analysis commands are present (ignores .tran/.ac)
- Use `faer` 0.24 for sparse matrix / linear algebra (pure Rust, supports LU via `FullPivLu`)
- Import `faer::prelude::Solve` trait to use `.solve()` on LU decomposition
- `SparseMatrix` uses triplet (COO) form with duplicate summing — standard for MNA stamp accumulation
- `LinearSystem` wraps `SparseMatrix` + RHS vector and provides `.solve()` -> `Result<Vec<f64>>`
- ferrospice-netlist already parses: R, C, L, V, I, D, Q, M, J, K, E, F, G, H, B, X elements
- ferrospice-netlist already parses: .op, .dc, .tran, .ac, .noise, .tf, .sens analyses
- Workspace deps: `thiserror` (workspace), `facet` (workspace), `approx` (dev)
- MNA assembly: `assemble_mna(&Netlist) -> Result<MnaSystem>` in ferrospice-core::mna
- DC OP solver: `simulate_op(&Netlist) -> Result<SimResult, MnaError>` in ferrospice-core::simulate
- SimResult vectors: node voltages as `v(node_name)`, branch currents as `vsource_name#branch` (lowercase)
- Node "0" is ground and excluded from the MNA matrix (NodeMap returns None for it)
- MNA matrix size = num_non_ground_nodes + num_voltage_sources
- Solution vector layout: [node voltages..., vsource branch currents...]
- SPICE current source convention: `I n+ n-` → current exits n+ externally, enters n-
- Use `stamp_conductance()` helper for resistor-like stamps between two nodes
- Inductor in DC is modeled as a 0V voltage source (branch equation), added to `vsource_names`
- Capacitor in DC has no MNA contribution — just register nodes, skip stamping
- `simulate_op` automatically includes inductor branch currents since they share `vsource_names`
- NR solver: `newton_raphson_solve(options, dim, num_nodes, load_system, initial_guess)` in ferrospice-core::newton
- `load_system` callback signature: `Fn(&[f64], &mut LinearSystem, f64)` — (solution, system, source_factor)
- NR convergence: voltages use `reltol * max(|old|,|new|) + vntol`, currents use `abstol` instead of `vntol`
- NR fallback chain: direct NR → Gmin stepping (1e-2 down to 1e-12) → source stepping (0.0 to 1.0)
- Diode companion model: g_d = IS/Vt * exp(V/Vt), i_eq = I(V) - g_d * V; stamp g_d as conductance, i_eq as current source
- `NrOptions::default()` matches ngspice: ABSTOL=1e-12, RELTOL=1e-3, VNTOL=1e-6, ITL1=100, ITL2=50, GMIN=1e-12
- Diode model: `DiodeModel` in ferrospice-core::diode — IS, N, RS, BV, IBV, CJO, VJ, M, TT params
- Diode companion: `(g_eq, i_eq) = model.companion(v_jct)` — stamp g_eq as conductance, i_eq as current source
- `DiodeModel::from_model_def(&ModelDef)` parses .model params; `.with_instance_params(&[Param])` applies overrides
- `DiodeInstance` in MNA: tracks anode_idx, cathode_idx, internal_idx (for RS>0), and resolved DiodeModel
- `stamp_conductance` is now public (used by both mna.rs and simulate.rs NR load function)
- `Triplet` struct and `SparseMatrix::triplets()` are public — NR load copies base linear stamps each iteration
- `SparseMatrixError::SingularMatrix(String)` variant wraps NrError messages
- `simulate_op` auto-detects diodes: linear circuits use direct solve, nonlinear use NR
- `solve_nonlinear_op(mna)` builds NR load closure that replays base stamps + diode companions
- Voltage limiting: `pnjlim(v_new, v_old, vt, vcrit)` prevents NR divergence from exp() overflow
- `collect_solution_into` and `simulate_dc` also detect diodes and use NR for each sweep point
- MOSFET Level 1: `MosfetModel` + `MosfetInstance` + `MosfetCompanion` + `stamp_mosfet()` in ferrospice-core::mosfet
- MOSFET follows BJT pattern: companion() computes linearized model, stamp_mosfet() applies to MNA
- MOSFET source-drain symmetry: mode=+1 (normal) or -1 (reversed when VDS<0)
- MOSFET body effect: Vth = VTO + GAMMA*(sqrt(PHI-VBS)-sqrt(PHI)), gmbs = gm * 0.5*GAMMA/sqrt(PHI-VBS)
- MOSFET mos_limit: VGS limited ±0.5V around VTO, VDS uses quadratic limiting for large changes
- MOSFET AC: gds/gm/gmbs real conductances + gate overlap caps (CGSO*W, CGDO*W, CGBO*L) + junction caps (CBD, CBS)
- MOSFET internal nodes: RD>0 creates internal drain, RS>0 creates internal source (like BJT series resistances)
- `has_nonlinear` checks: `!mna.diodes.is_empty() || !mna.bjts.is_empty() || !mna.mosfets.is_empty()`
---

## 2026-03-08 - US-001
- Created `ferrospice-core` crate with `SparseMatrix` (triplet/COO form) and `LinearSystem` types
- Uses `faer` 0.24 for LU factorization (`FullPivLu`) and solve
- 5 unit tests: 3x3 system solve, triplet duplicate summing, identity, singular, empty
- Fixed pre-existing clippy warnings in ferrospice-netlist (manual_strip, unnecessary_to_owned)
- Files: ferrospice-core/Cargo.toml, ferrospice-core/src/lib.rs, ferrospice-core/src/sparse.rs, Cargo.toml
- **Learnings for future iterations:**
  - `faer` requires explicit `use faer::prelude::Solve` to call `.solve()` on decompositions
  - ferrospice-netlist has comprehensive parser already — elements, analyses, subcircuits all parsed
  - Rust 1.94 edition 2024 is in use
---

## 2026-03-08 - US-002
- Created `ferrospice-core/src/mna.rs` with MNA matrix assembly from netlist
- `assemble_mna(&Netlist) -> Result<MnaSystem>` builds LinearSystem from parsed elements
- `NodeMap` maps node names to matrix indices, ground "0" excluded
- Stamps implemented: resistor (conductance), voltage source (branch equation), current source (RHS)
- `MnaSystem::solve()` returns `MnaSolution` with `.voltage(node)` and `.branch_current(vsrc)` accessors
- 5 unit tests: voltage divider, current source, series resistors, ground exclusion, multiple sources
- Files: ferrospice-core/Cargo.toml, ferrospice-core/src/lib.rs, ferrospice-core/src/mna.rs
- **Learnings for future iterations:**
  - SPICE current source convention: `I n+ n-` means current flows from n+ to n- externally (exits n+, enters n-)
  - For KCL stamps: subtract current from n+ RHS, add to n- RHS
  - Rust 1.94 requires explicit lifetime `'_` in return types when elided elsewhere (mismatched_lifetime_syntaxes)
  - `ferrospice-core` now depends on `ferrospice-netlist` for the Netlist/Element types
---

## 2026-03-08 - US-003
- Created `ferrospice-core/src/simulate.rs` with `simulate_op(&Netlist) -> Result<SimResult, MnaError>`
- Assembles MNA, solves, returns SimResult with node voltages and branch currents
- 3 unit tests: voltage divider (V(mid)=2.5V), series resistors with current source, res_simple.cir port
- SimResult format: single plot "op1", vectors named "v(node)" and "vsource#branch"
- Files: ferrospice-core/src/simulate.rs, ferrospice-core/src/lib.rs
- **Learnings for future iterations:**
  - SimResult/SimPlot/SimVector types live in ferrospice-netlist, not ferrospice-core
  - Branch current vector naming convention: lowercase source name + "#branch" (e.g., "v1#branch")
  - res_simple.cir uses .TRAN but the DC OP result is I(V1) = -0.0001A (V=1V, R=10kΩ)
---

## 2026-03-08 - US-004
- Added capacitor and inductor DC stamps to MNA assembly in `ferrospice-core/src/mna.rs`
- Capacitor: open circuit in DC (registers nodes only, no matrix contribution)
- Inductor: short circuit in DC (zero-voltage branch equation, reuses voltage source stamp pattern)
- Added 2 tests: `test_rc_circuit_dc_op` and `test_rl_circuit_dc_op`
- Files: ferrospice-core/src/mna.rs
- **Learnings for future iterations:**
  - Inductor DC stamp is identical to a voltage source stamp with V=0 — reuse the same pattern
  - Inductor branch entries go into `vsource_names`, so `simulate_op` picks them up automatically
  - No changes needed in `simulate.rs` — the existing loop over `vsource_names` handles inductors
---

## 2026-03-08 - US-005
- Created integration test module `ferrospice-core/tests/resistance.rs`
- Loads actual .cir files from `ngspice-upstream/tests/resistance/` directory
- Validates DC OP results against numerical values from .out reference files
- Created `filter_output()` utility matching ngspice check.sh filtering rules
- Created `extract_data_rows()` for parsing tab-separated data from .out files
- Tests: res_simple DC OP (pass), res_partition DC OP (pass), res_array (#[ignore] - needs model support)
- Full output comparison tests marked #[ignore] with TODOs for transient (US-009) and AC (US-012)
- Files: ferrospice-core/tests/resistance.rs (new), ferrospice-core/src/mna.rs (fmt)
- **Learnings for future iterations:**
  - `simulate_op` works on any netlist even without explicit .OP command — it just builds MNA and solves
  - Use `CARGO_MANIFEST_DIR` + relative path to locate ngspice-upstream test files from integration tests
  - ngspice .out data rows: tab-separated, index column first, scientific notation values
  - check.sh filtering is quite aggressive — removes analysis keywords, headers, solver info
  - res_array.cir needs model-based resistance (RSH, W/L geometry) — R = RSH * L / (W - NARROW) for R3=11kΩ
  - res_partition.cir uses ac=15k on r2 — changes AC behavior (V(2)=0.75) vs DC (V(2)=0.5)
---

## 2026-03-08 - US-006
- Implemented `simulate_dc(&Netlist) -> Result<SimResult>` for DC sweep analysis
- Supports single sweep (.dc V1 0 5 1) and double/nested sweep (.dc V1 0 5 1 V2 0 3 1)
- Supports sweeping both voltage sources and current sources
- Reuses MNA matrix assembly — only modifies RHS entries per sweep point for efficiency
- Result includes sweep variable vector + node voltages + branch currents
- Added `SweepSource` enum to track voltage vs current source sweep info
- Made `NodeMap::len()` public and added `NodeMap::is_empty()` for clippy compliance
- Files: ferrospice-core/src/simulate.rs, ferrospice-core/src/mna.rs, ferrospice-core/src/lib.rs
- **Learnings for future iterations:**
  - For voltage source sweep, only RHS[branch_idx] changes — very efficient
  - For current source sweep, need to track original DC value to undo/redo RHS contributions
  - Double sweep: outer=src2, inner=src1 (matches ngspice behavior)
  - `generate_sweep_points()` handles positive/negative step directions
  - `NodeMap::len()` was private — needed to make public for simulate.rs access
---

## 2026-03-08 - US-007
- Created `ferrospice-core/src/newton.rs` with Newton-Raphson nonlinear solver
- `newton_raphson_solve()` with configurable `NrOptions` (ABSTOL, RELTOL, VNTOL, ITL1, ITL2, GMIN)
- Convergence check: ngspice-style absolute+relative criteria, separate for voltages vs currents
- Fallback chain: direct NR → Gmin stepping (1e-2 → 1e-12 by 10x) → source stepping (0→1 in 10 steps)
- `NrAttempt` struct to avoid too-many-arguments clippy warning on internal `try_nr`
- 4 tests: diode circuit convergence (<20 iters), linear system (2 iters), convergence check, high-voltage diode
- Files: ferrospice-core/src/newton.rs (new), ferrospice-core/src/lib.rs
- **Learnings for future iterations:**
  - Diode companion model: g_d = IS/Vt * exp(V/Vt), i_eq = I(V) - g_d * V
  - Stamp companion as conductance (g_d to matrix) + current source (i_eq to RHS)
  - NR KCL residual accuracy limited by RELTOL (~0.1%), don't assert tighter than ~1e-4 for currents
  - `load_system` callback takes source_factor (f64) for source stepping support
  - Clippy enforces max 7 arguments per function — use structs to bundle params
  - ngspice ITL2=50 is used for Gmin/source stepping sub-iterations
---

## 2026-03-08 - US-008
- Created `ferrospice-core/src/diode.rs` with `DiodeModel` struct and Shockley equation
- Model params: IS, N, RS, BV, IBV, CJO, VJ, M, TT — all with ngspice defaults
- NR companion model: `companion(v) -> (g_eq, i_eq)` for linearization at each iteration
- Voltage limiting via `pnjlim()` matching ngspice `DEVpnjlim` for convergence stability
- Series resistance support: RS > 0 creates internal node between RS and junction
- `DiodeInstance` in MNA tracks resolved node indices and model params
- Modified `assemble_mna()` to parse .model defs, create `DiodeInstance`s, skip diode stamps (applied during NR)
- Modified `simulate_op()` to auto-detect nonlinear elements and use NR solver
- `solve_nonlinear_op()` builds NR load closure: replays base linear stamps + diode companions
- Modified `collect_solution_into()` and `simulate_dc()` to use NR for nonlinear circuits
- Made `stamp_conductance`, `Triplet`, `SparseMatrix::triplets()` public for NR load function
- Added `SparseMatrixError::SingularMatrix(String)` variant to wrap NR errors
- 10 new tests: DiodeModel unit tests (5), simulate_op diode tests (2), DC sweep I-V tests (2), Shockley verification (1)
- Files: ferrospice-core/src/diode.rs (new), ferrospice-core/src/lib.rs, ferrospice-core/src/mna.rs, ferrospice-core/src/simulate.rs, ferrospice-core/src/sparse.rs
- **Learnings for future iterations:**
  - NR load function needs to copy base linear stamps each iteration (matrix is cleared) — use `triplets()` accessor
  - `RefCell` needed for mutable state (prev junction voltages) inside `Fn` closure for NR
  - Model lookup: build `BTreeMap<String, &ModelDef>` keyed on uppercase name during MNA assembly
  - Internal nodes (for RS) are indexed after external nodes: `node_map.len()..node_map.len()+internal_count`
  - `num_nodes` for NR convergence check must include internal nodes, not just external ones
  - DC sweep with diodes: each sweep point runs full NR solve, not just linear matrix solve
---

## 2026-03-08 - US-009
- Created `ferrospice-core/src/transient.rs` with transient analysis engine
- `simulate_tran(&Netlist) -> Result<SimResult>` — full .tran analysis
- Backward Euler for first timestep, Trapezoidal for subsequent (matches ngspice)
- Capacitor companion model: geq conductance + ieq history current source
- Inductor companion model: req on branch diagonal + veq history voltage on RHS
- Added `CapacitorInstance` and `InductorInstance` to `MnaSystem` with IC= param support
- Refactored `assemble_mna` to collect C/L instances directly (not via `stamp_element`)
- Added `stamp_inductor_topology()` and `extract_ic_param()` helpers in mna.rs
- NR integration for nonlinear transient (diodes work during transient)
- 2 tests: RC step response (within 1% at 1-5 RC), LC oscillator (frequency within 1%)
- Files: ferrospice-core/src/transient.rs (new), ferrospice-core/src/mna.rs, ferrospice-core/src/lib.rs
- **Learnings for future iterations:**
  - Capacitor BE companion: geq=C/h, ieq=-C/h*v_prev; Trap: geq=2C/h, ieq=-(2C/h*v_prev+i_prev)
  - Inductor BE companion: req=L/h, veq=-L/h*i_prev; Trap: req=2L/h, veq=-(2L/h*i_prev+v_prev)
  - IC= parameter on C/L elements overrides DC OP initial conditions for transient
  - Inductor topology stamps (1/-1 entries) are shared between DC and transient — only req/veq differ
  - History current for capacitor must be computed from the companion model equation, not just C*dv/dt
  - For LC oscillator test, trapezoidal method preserves energy well — no amplitude decay over 2 cycles
  - `#[expect(clippy::too_many_arguments)]` needed for `solve_timestep` with 11 params
---

## 2026-03-08 - US-010
- Created `ferrospice-core/src/waveform.rs` with evaluation functions for all 6 waveform types
- PULSE, SIN, EXP, PWL, SFFM, AM — all matching ngspice semantics from vsrcload.c
- Added `VoltageSourceInstance` and `CurrentSourceInstance` structs to `MnaSystem` (mna.rs)
- Source instances track branch_idx/node_indices, DC value, and optional waveform
- Modified `stamp_element()` to collect source instances during MNA assembly
- Modified `solve_timestep()` in transient.rs to evaluate waveforms at current time and override RHS
- Voltage source waveform: replaces `system.rhs[branch_idx]` with evaluated value
- Current source waveform: adjusts node RHS entries by `(i_transient - i_dc)` difference
- 10 waveform unit tests: PULSE, SIN (basic, offset+phase, damping, analytical), EXP, PWL, SFFM, AM
- 4 transient integration tests: PULSE into RC, SIN with R load, PWL source, current source PULSE
- Files: ferrospice-core/src/waveform.rs (new), ferrospice-core/src/lib.rs, ferrospice-core/src/mna.rs, ferrospice-core/src/transient.rs
- **Learnings for future iterations:**
  - Waveform evaluation is pure math — no MNA dependency, easy to unit test in isolation
  - Current source waveforms need to compute delta from DC value (undo DC stamp, apply transient)
  - SIN phase is in degrees (ngspice convention), converted to radians internally
  - PULSE default period = tr + pw + tf (if per not specified or <= 0)
  - SFFM modulation depth is clamped to [0, fc/fs] (ngspice enforces this with warning)
  - AM waveform returns 0 before td (not the offset — matching ngspice)
  - `stamp_element()` now takes `voltage_sources` and `current_sources` vecs — needs `#[expect(clippy::too_many_arguments)]`
  - `TranParams` struct bundles tstep and tstop for waveform default computation
---

## 2026-03-08 - US-011
- Added adaptive timestep control to transient analysis engine
- LTE estimation using difference between BE and Trap integration for capacitor charges and inductor fluxes
- `BreakpointTable` struct: collects breakpoint times from waveforms, prevents timestep from crossing discontinuities
- `waveform::breakpoints()` function: extracts breakpoint times from PULSE edges, PWL corners, EXP/SIN/AM transitions
- At breakpoints: use BE integration and reduce step by 0.1× for stability (matching ngspice behavior)
- Timestep adjustment: reject step if new_h < 0.9×step_h (too much LTE), grow up to 2× max
- `tmax` from .tran command respected as upper bound on internal timestep
- Default h_max = min(tstep, tstop/50) when tmax not specified
- Separate `step_h` (actual step used) from `h` (suggested next step) to avoid state confusion
- Test: `test_adaptive_timestep_pulse_rc` verifies accuracy and step placement near edges
- Ported `fourbitadder.cir` as `#[ignore]` integration test (needs BJT + subcircuit support)
- Files: ferrospice-core/src/transient.rs, ferrospice-core/src/waveform.rs, ferrospice-core/tests/transient.rs (new)
- **Learnings for future iterations:**
  - step_h vs h distinction is critical: step_h is the actual step for this iteration, h is the LTE-suggested next step
  - Breakpoints must be collected before the time loop starts — iterating waveform params during stepping is too slow
  - PULSE breakpoints repeat every period — need a loop with safety limit to avoid infinite iteration with tiny periods
  - ngspice TRTOL=7.0 is a "slack factor" on LTE tolerance — without it, timestep is too conservative
  - CHGTOL=1e-14 prevents zero-charge elements from demanding infinitely small timesteps
  - At breakpoints, BE is essential — Trap can oscillate (ringing) at discontinuities
  - The `is_at_breakpoint()` check uses `min_break` tolerance to avoid floating-point near-miss issues
---

## 2026-03-08 - US-012
- Created `ferrospice-core/src/ac.rs` with AC small-signal analysis engine
- `simulate_ac(&Netlist) -> Result<SimResult>` — full .ac frequency sweep analysis
- Added `ComplexLinearSystem` to sparse.rs — separate real/imag sparse matrices solved via faer c64 LU
- AC algorithm: DC OP → linearize → build (G + jωC) matrix → sweep frequency → solve at each point
- AC stamps: resistors (real), capacitors (jωC imaginary), inductors (-jωL on branch diagonal)
- Diode AC stamps: small-signal conductance + junction capacitance + transit time capacitance
- Added `DiodeModel::junction_capacitance(v)` for depletion cap with linear extrapolation beyond FC*VJ
- AC source excitation: AcSpec mag/phase → complex RHS (real=mag*cos(phase), imag=mag*sin(phase))
- Frequency sweep: DEC (n pts/decade), OCT (n pts/octave), LIN (n total points)
- Result: "frequency" real vector + complex node voltages + complex branch currents
- 7 unit tests: sweep generation (DEC/OCT/LIN), RC lowpass -3dB, RL highpass
- 2 integration tests: ngspice lowpass.cir port (31 points matched), RC -3dB analytical verification
- Files: ferrospice-core/src/ac.rs (new), ferrospice-core/src/sparse.rs, ferrospice-core/src/diode.rs, ferrospice-core/src/lib.rs, ferrospice-core/tests/ac.rs (new)
- **Learnings for future iterations:**
  - faer supports `c64` type natively — `Mat::<c64>::zeros()` and `FullPivLu` work with complex matrices
  - AC analysis reuses DC MNA stamps for the real (conductance) part — only add imaginary susceptances
  - Inductor AC stamp goes on the branch diagonal (imaginary), not on node entries
  - Diode junction capacitance uses forward coefficient FC=0.5 and linear extrapolation to avoid singularity at V=VJ
  - AC source phase is in degrees (from AcSpec), must convert to radians for cos/sin decomposition
  - The lowpass.cir test has V1 with both AC and SIN waveform — `simulate_ac` only uses the AC spec, ignores transient waveform
  - ComplexLinearSystem keeps real and imag matrices separate (cleaner than interleaved)
- BJT model in `ferrospice-core::bjt`: BjtModel (params), BjtInstance (resolved nodes), BjtCompanion (NR result), stamp_bjt()
- BJT decomposition: gpi (b'-e'), gmu (b'-c'), gm VCCS (V_be→I from e' to c'), go (c'-e'), plus series resistances
- PNP: `BjtType::sign()` returns -1.0, applied to junction voltage extraction and RHS equivalent currents
- BJT internal nodes: `model.internal_node_count()` returns count based on RB/RC/RE > 0
- BJT junction capacitances: `junction_cap(v, cj0, vj, m)` with FC=0.5 linear extrapolation
- BJT NR convergence: separate pnjlim for VBE and VBC with independent vcrit values
---

## 2026-03-08 - US-013
- Implemented Ebers-Moll/Gummel-Poon level 1 BJT model (NPN and PNP)
- New file: `ferrospice-core/src/bjt.rs` — BjtModel, BjtInstance, BjtCompanion, stamp_bjt
- Modified: `mna.rs` (BJT parsing/assembly), `simulate.rs` (OP + DC sweep NR), `ac.rs` (small-signal + caps), `transient.rs` (NR + companion)
- Files changed: bjt.rs (new), lib.rs, mna.rs, simulate.rs, ac.rs, transient.rs
- **Learnings for future iterations:**
  - BJT decomposes into 4 elements: gpi (b'-e'), gmu (b'-c'), gm VCCS (V_be→I c'-e'), go (c'-e')
  - PNP handled via type sign multiplier: junction_voltages() returns sign*(V_bp-V_ep), RHS ceqbe/ceqbc scaled by sign
  - Base charge normalization (Gummel-Poon): q1 from Early effect, qb from high-injection rolloff (IKF/IKR)
  - Series resistances (RB, RC, RE) create internal nodes — `internal_node_count()` determines count
  - BJT junction voltage limiting reuses `pnjlim()` from diode module, separate vcrit for BE and BC
  - AC BJT: real conductances (gpi, gmu, gm, go) + imaginary junction capacitances (CJE+TF*gbe, CJC+TR*gbc)
  - IS=1e-16 gives VBE ≈ 0.81V (higher than textbook 0.7V) — use wider tolerance in tests
  - DC sweep VCE branch current is negative (source supplies current) — use .abs() in tests
---

## 2026-03-08 - US-014
- Implemented MOSFET Level 1 (Shichman-Hodges) model for NMOS and PMOS
- New file: `ferrospice-core/src/mosfet.rs` — MosfetModel, MosfetInstance, MosfetCompanion, stamp_mosfet, mos_limit
- Modified: `lib.rs` (exports), `mna.rs` (MOSFET parsing/assembly), `simulate.rs` (OP + DC sweep NR + 4 tests), `ac.rs` (small-signal + caps), `transient.rs` (NR + companion)
- Files changed: mosfet.rs (new), lib.rs, mna.rs, simulate.rs, ac.rs, transient.rs
- **Learnings for future iterations:**
  - MOSFET follows same pattern as BJT: companion() → stamp_mosfet(), type sign for NMOS/PMOS
  - Source-drain symmetry: mode=+1 (normal) or mode=-1 (reversed, when VDS<0), swap internal drain/source
  - Body effect: Vth = VTO + GAMMA * (sqrt(PHI - VBS) - sqrt(PHI)), derivative arg = 0.5*GAMMA/sqrt(PHI-VBS)
  - gmbs (body transconductance) = gm * arg, stamps between source and bulk
  - Shichman-Hodges: beta = KP * W / L_eff, L_eff = L - 2*LD
  - Three regions: cutoff (Vgst≤0), saturation (Vds≥Vgst), linear (Vds<Vgst)
  - Channel length modulation: multiply by (1 + LAMBDA * Vds)
  - Bulk junction diodes at drain-bulk and source-bulk (gbd, gbs) use Shockley equation
  - Series resistances (RD, RS > 0) create internal drain/source nodes
  - AC stamps: gds, gm, gmbs (real) + CGSO*W, CGDO*W, CGBO*L (gate overlap caps, imaginary) + CBD, CBS (junction caps, imaginary)
  - mos_limit: separate limiting for VGS (±0.5V step limit around VTO) and VDS (quadratic limit for large changes)
  - Bulk diode leakage causes small drain voltage drop even in cutoff — use relaxed tolerances in tests
- JFET model: `JfetModel::companion(vgs, vgd) -> JfetCompanion` with Sydney University modified Shichman-Hodges
- JFET Bfac in saturation: `Bfac = vgst * Bfac` (modified before computing gm/cpart)
- JFET gate junctions: PN diode leakage (ggs, ggd) via `gate_junction_current()` with deep-reverse-bias cube approximation
- `stamp_jfet()` stamps ggs, ggd, gds conductances + gm VCCS + series R + RHS current sources
- JFET internal nodes: RD > 0 creates drain_prime, RS > 0 creates source_prime
---

## 2026-03-08 - US-015
- Implemented Level 1 JFET model with Sydney University modification
- Files changed: ferrospice-core/src/jfet.rs (new), ferrospice-core/tests/jfet.rs (new), ferrospice-core/src/lib.rs, ferrospice-core/src/mna.rs, ferrospice-core/src/simulate.rs, ferrospice-core/src/ac.rs, ferrospice-core/src/transient.rs
- **Learnings for future iterations:**
  - JFET Sydney University modification: `Bfac = (1-b)/(PB-VTO)`, b defaults to 1.0 (simplifies to standard quadratic)
  - In saturation, ngspice modifies Bfac: `Bfac = vgst * Bfac` before computing gm and cpart — this is critical
  - Linear region cpart formula: `vds*(vds*(Bfac*vds - b) + vgst*apart)` — NOT `vds*(vgst*apart - Bfac*vds²)`
  - Linear region gm: `betap*vds*(apart + 3*Bfac*vgst)` — includes extra `3*Bfac*vgst` term
  - Inverse mode uses `betap = beta*(1 - lambda*vds)` (note: vds is negative, so this increases beta)
  - Inverse mode gds includes `-gm` term; inverse saturation gds = `lambda*beta*cpart - gm`
  - JFET gate junctions are PN diodes (Shockley equation) — same gate_junction_current for GS and GD
  - DC sweep source indexing must account for ALL internal nodes (diodes, BJTs, MOSFETs, JFETs) via total_num_nodes()
  - Always read the actual ngspice C source (e.g., jfetload.c) rather than implementing from textbook equations — ngspice has specific modifications
---
